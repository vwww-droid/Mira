#!/usr/bin/env bash
set -euo pipefail
ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "${ROOT_DIR}"
if [[ "${1:-}" == foreground ]]; then
  shift
  exec "${ROOT_DIR}/tools/relay/start-public-relay.sh" "$@"
fi
exec "${PYTHON_BIN:-python3}" -m mira.relay.managed "$@"
